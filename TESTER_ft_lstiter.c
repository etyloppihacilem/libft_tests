/*
TEST FILE FOR ft_lstclear_bonus.c
Generated with the test_generator utility
*/

// Write your dependencies after the :
// do not uncomment
//DEPENDENCIES: libft.a

#include <stdio.h> // do not remove please
#include "../libft.h"

void ft_lstiter(t_list *lst, void (*f)(void *));

void	deletion(void *a)
{
	*((int*)a) = 0;
}

// Autogenerated test function for ft_lstclear
int	T_ft_lstiter(void)
{
	t_list	*a;
	t_list	*b;
	t_list	*c;
	t_list	*d;
	t_list	*e;
	t_list	*f;
	int		i;
	int		j;
	int		k;
	int		l;
	int		m;

	i = 1;
	j = 2;
	k = 3;
	l = 5;
	m = 12;
	a = ft_lstnew(&i);
	b = ft_lstnew(&j);
	c = ft_lstnew(&k);
	d = ft_lstnew(&l);
	e = ft_lstnew(&m);

	f = a;
	ft_lstadd_back(&f, b);
	ft_lstadd_back(&f, c);
	ft_lstadd_back(&f, d);
	ft_lstadd_back(&f, e);

	ft_lstiter(a, NULL);
	ft_lstiter(NULL, &deletion);
	ft_lstiter(NULL, NULL);
	ft_lstiter(a, &deletion);
	int	to_return;
	to_return = (i + j + k + l + m);
	ft_lstclear(&a, &deletion);
	return (to_return);
}
